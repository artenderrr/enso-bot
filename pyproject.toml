[project]
name = "enso-bot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.11.18",
    "pytelegrambotapi>=4.26.0",
    "python-dotenv>=1.1.0",
]

[tool.mypy]
strict = true
